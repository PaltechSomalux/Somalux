# Faculty Tracking - Visual Architecture

## Before vs After

### BEFORE (Problem)
```
Browser A (User 1)           Browser B (User 1)
┌─────────────────┐         ┌─────────────────┐
│ Engineering ♥ 3 │         │ Engineering ♥ 1 │
│ localStorage    │         │ localStorage    │
│ [isolated]      │         │ [isolated]      │
└─────────────────┘         └─────────────────┘
       ❌ Counts don't match!

Browser C (User 2)
┌─────────────────┐
│ Engineering ♥ 2 │
│ localStorage    │
│ [isolated]      │
└─────────────────┘
       ❌ Different count per user!
```

### AFTER (Solution)
```
Browser A (User 1)   Browser B (User 1)   Browser C (User 2)
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│Engineering ♥ │    │Engineering ♥ │    │Engineering ♥ │
│    5         │    │    5         │    │    5         │
└──────────────┘    └──────────────┘    └──────────────┘
       │                   │                   │
       └─────────┬─────────┴───────────┬───────┘
                 ↓                     ↓
          ┌──────────────────────────────────┐
          │   Supabase Database              │
          │  (Single Source of Truth)        │
          ├──────────────────────────────────┤
          │ faculty_likes table:             │
          │  • User1 → Engineering           │
          │  • User2 → Engineering           │
          │  • User2 → Science               │
          │  (Total: 2 likes for Engineer)   │
          └──────────────────────────────────┘
       ✅ All users see same count (5)
       ✅ Aggregated across all users
       ✅ Persists across browser refreshes
```

## Data Flow Diagram

### Faculty Selection (View Tracking)

```
User clicks "Engineering" faculty card
              ↓
   handleFacultySelect("Engineering")
              ↓
   ┌─────────────────────────────────┐
   │ Is user authenticated?          │
   └──────┬────────────────┬─────────┘
          │ YES            │ NO
          ↓                ↓
    ┌──────────────┐  ┌──────────────┐
    │ Update DB:   │  │ localStorage │
    │ faculty_views│  │    only      │
    └──────┬───────┘  └──────┬───────┘
           │                 │
           └────────┬────────┘
                    ↓
        Update local state
                    ↓
        FacultyGridDisplay
        (shows view count)
                    ↓
          User browses papers
```

### Faculty Like Toggle (Like Tracking)

```
User clicks heart icon on "Engineering" card
              ↓
   handleToggleFacultyLike("Engineering")
              ↓
   Is user authenticated? (NO → return early)
              ↓
   ┌──────────────────────────────────┐
   │ Is already liked?                │
   └──────┬────────────────┬──────────┘
          │ YES            │ NO
          ↓                ↓
    ┌──────────────┐  ┌──────────────┐
    │ DELETE from  │  │ INSERT into  │
    │ faculty_likes│  │ faculty_likes│
    └──────┬───────┘  └──────┬───────┘
           │                 │
           └────────┬────────┘
                    ↓
        Update local like state
                    ↓
        Query get_faculty_like_counts()
        (RPC function)
                    ↓
        Update like count state
                    ↓
        FacultyGridDisplay
        (shows updated like count)
```

### Page Load (Data Initialization)

```
User logs in / User changes
              ↓
          setUser()
              ↓
   useEffect [user?.id] triggered
              ↓
   loadFacultyData() called
              ↓
   ┌─────────────────────┐
   │ Is user set?        │
   └────┬────────────┬───┘
        │ NO         │ YES
        ↓            ↓
   ┌─────────┐  ┌─────────────────────────┐
   │Load from│  │ Query Supabase:         │
   │localStorage│  │ 1. faculty_views       │
   └────┬────┘  │ 2. faculty_likes       │
        │       │ 3. get_faculty_like... │
        │       └────────┬────────────────┘
        │                ↓
        │       Get user's views/likes
        │       and aggregated counts
        │                ↓
        └────────┬───────┘
                 ↓
        Update state:
        • facultyViews
        • facultyLikes
        • facultyLikesCounts
                 ↓
        FacultyGridDisplay re-renders
        (shows correct counts)
```

## Database Schema Visualization

```
┌──────────────────────────────────────────────────────────┐
│                    Supabase PostgreSQL                    │
└──────────────────────────────────────────────────────────┘

┌─ faculty_views table ─────────────────────────────────────┐
│                                                            │
│  user_id | faculty_name | views | updated_at              │
│  ─────────────────────────────────────────────            │
│  uuid-1  | Engineering  | 3     | 2024-01-15 10:30:00    │
│  uuid-1  | Science      | 1     | 2024-01-15 09:15:00    │
│  uuid-2  | Engineering  | 2     | 2024-01-14 14:20:00    │
│  uuid-2  | Medicine     | 4     | 2024-01-15 11:00:00    │
│  uuid-3  | Engineering  | 1     | 2024-01-15 12:45:00    │
│                                                            │
│  Index: (user_id, faculty_name)  [UNIQUE]                │
│  Index: user_id  [For fast user queries]                 │
│  Index: faculty_name  [For analytics]                    │
│                                                            │
└────────────────────────────────────────────────────────────┘

┌─ faculty_likes table ─────────────────────────────────────┐
│                                                            │
│  id  | user_id | faculty_name | created_at                │
│  ──────────────────────────────────────────────          │
│  pk1 | uuid-1  | Engineering  | 2024-01-15 10:30:00     │
│  pk2 | uuid-2  | Engineering  | 2024-01-14 14:20:00     │
│  pk3 | uuid-2  | Science      | 2024-01-15 09:00:00     │
│  pk4 | uuid-3  | Engineering  | 2024-01-15 12:45:00     │
│  pk5 | uuid-1  | Medicine     | 2024-01-15 11:30:00     │
│                                                            │
│  Unique: (user_id, faculty_name)  [One like per user]    │
│  Index: user_id  [For user queries]                      │
│  Index: faculty_name  [For aggregation]                  │
│                                                            │
└────────────────────────────────────────────────────────────┘

┌─ RPC Function: get_faculty_like_counts() ─────────────────┐
│                                                            │
│  SELECT faculty_name, COUNT(*) as count                  │
│  FROM faculty_likes                                       │
│  GROUP BY faculty_name                                    │
│  ORDER BY faculty_name                                    │
│                                                            │
│  Returns:                                                │
│  ┌─────────────────────────────────────┐                │
│  │ faculty_name | count                │                │
│  │ ─────────────────────────────       │                │
│  │ Engineering  | 3                    │                │
│  │ Medicine     | 1                    │                │
│  │ Science      | 1                    │                │
│  └─────────────────────────────────────┘                │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

## State Synchronization Flow

```
┌─────────────────────────────────────────────────────────────┐
│                   React Component State                     │
│                   (Pastpapers.jsx)                         │
└─────────────────────────────────────────────────────────────┘

┌─ State Variables ──────────────────────────────────────────┐
│                                                             │
│  facultyViews: {                                           │
│    "Engineering": 3,     ← My view count (per user)        │
│    "Science": 1,                                           │
│    "Medicine": 4                                           │
│  }                                                          │
│                                                             │
│  facultyLikes: {                                           │
│    "Engineering": true,  ← Did I like this?                │
│    "Science": false,                                       │
│    "Medicine": false                                       │
│  }                                                          │
│                                                             │
│  facultyLikesCounts: {                                     │
│    "Engineering": 3,     ← Total likes (all users)         │
│    "Science": 1,                                           │
│    "Medicine": 1                                           │
│  }                                                          │
│                                                             │
└─────────────────────────────────────────────────────────────┘

                          ↕ (sync)

┌─────────────────────────────────────────────────────────────┐
│              Browser localStorage (Fallback)               │
│  (Used for offline + anonymous users)                      │
└─────────────────────────────────────────────────────────────┘

                          ↕ (primary)

┌─────────────────────────────────────────────────────────────┐
│           Supabase Database (Source of Truth)              │
│  (Persistent across browsers, shared across users)         │
└─────────────────────────────────────────────────────────────┘
```

## Component Rendering Logic

```
FacultyGridDisplay Component
┌──────────────────────────────────────────────────┐
│                                                  │
│  filteredFaculties.map(faculty => (              │
│    <motion.div key={faculty}>                   │
│      ┌────────────────────────────────────┐     │
│      │  Card Header (Green Gradient)      │     │
│      │  Faculty Name Initial              │     │
│      ├────────────────────────────────────┤     │
│      │  Faculty Name                      │     │
│      │  "Engineering" → 140px section     │     │
│      ├────────────────────────────────────┤     │
│      │           FOOTER                   │     │
│      │  ┌──────────────────────────┐     │     │
│      │  │ 👁  3        📄 24       │     │     │
│      │  │ Views   PaperCount       │     │     │
│      │  │      ♥ 5                 │     │     │
│      │  │      LikeCount           │     │     │
│      │  │      (from DB)           │     │     │
│      │  │                          │     │     │
│      │  │ onClick: ♥               │     │     │
│      │  │ → handleToggleFacultyLike│     │     │
│      │  │ → Syncs to DB            │     │     │
│      │  │ → Updates count          │     │     │
│      │  └──────────────────────────┘     │     │
│      └────────────────────────────────────┘     │
│                                                  │
│      onClick on card:                           │
│      → handleFacultySelect(faculty)             │
│      → Increments views in DB                   │
│      → Closes grid                              │
│      → Shows papers for faculty                 │
│                                                  │
│    ))                                           │
│                                                  │
└──────────────────────────────────────────────────┘

Data Sources Used:
✓ facultyViews[faculty]     → Eye icon count
✓ paperCount               → Paper icon count  
✓ facultyLikesCounts[faculty] → Heart icon count
✓ facultyLikes[faculty]    → Heart filled/unfilled
```

## RLS Policy Model

```
┌────────────────────────────────────────────────────────────┐
│             Row Level Security (RLS) Policies              │
└────────────────────────────────────────────────────────────┘

faculty_views table:
┌──────────────────────────────────────────────────────┐
│ User 1 (uuid-1) can:                                 │
│  ✓ SELECT rows WHERE user_id = 'uuid-1'             │
│  ✓ INSERT rows WHERE user_id = 'uuid-1'             │
│  ✓ UPDATE rows WHERE user_id = 'uuid-1'             │
│  ✗ See rows WHERE user_id = 'uuid-2' (blocked by RLS)│
│  ✗ Modify rows WHERE user_id = 'uuid-2'             │
└──────────────────────────────────────────────────────┘

faculty_likes table:
┌──────────────────────────────────────────────────────┐
│ User 1 (uuid-1) can:                                 │
│  ✓ SELECT ALL rows (needed for aggregation)         │
│  ✓ INSERT rows WHERE user_id = 'uuid-1'             │
│  ✓ DELETE rows WHERE user_id = 'uuid-1'             │
│  ✗ See user_id column (not restricted, but row data is)|
│  ✗ Insert/delete rows WHERE user_id = 'uuid-2'      │
└──────────────────────────────────────────────────────┘

Result:
✅ User A cannot see User B's view history
✅ User A cannot like for User B  
✅ User A cannot unlike User B's likes
✅ Aggregated counts are public (everyone sees total)
```

## Deployment Architecture

```
Local Development
    ↓ (git push)
GitHub Repository
    ↓ (deploy)
Production Environment
    │
    ├─→ Frontend: Next.js/React on Vercel
    │   └─→ Pastpapers.jsx with DB calls
    │
    └─→ Backend: Supabase (PostgreSQL)
        ├─→ faculty_views table
        ├─→ faculty_likes table
        └─→ get_faculty_like_counts() RPC

Data Flow in Production:
User Browser
    ↓ HTTP API calls
Vercel Edge/Serverless
    ↓ Supabase client library
Supabase REST API
    ↓ SQL queries
PostgreSQL Database
    ↓ Results
Supabase REST API
    ↓ JSON response
Vercel/Browser
    ↓ Update state + localStorage
FacultyGridDisplay
    ↓ Render new counts
Updated UI
```

---

This visual guide helps understand:
1. **Before/After**: How data consistency is fixed
2. **Data Flow**: How views and likes are tracked
3. **Database**: What data is stored where
4. **State**: How React state syncs with database
5. **Rendering**: How counts appear in UI
6. **Security**: How RLS protects user data
7. **Deployment**: How code reaches production
